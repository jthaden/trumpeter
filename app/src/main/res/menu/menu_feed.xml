<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context="jthd.trumpeter.FeedActivity">
    <item android:id="@+id/settingsAction"
          android:title="@string/action_settings"
          android:orderInCategory="100"
          app:showAsAction="ifRoom"
          android:icon="@drawable/settings_actionbar_button"/>
      <item android:id="@+id/notificationsAction"
            android:title="@string/notifications"
            android:orderInCategory="10"
            android:icon="@drawable/notifications_trumpet"
            app:showAsAction="ifRoom"/>
      <item android:id="@+id/refreshAction"
          app:showAsAction="ifRoom"
          android:title="Refresh"
          android:icon="@drawable/refresh_actionbar_button"/>
    <item android:id="@+id/signOutAction"
          app:showAsAction="never"
          android:title="Sign Out"/>
</menu>
